# Modfolio Improvements TODO

## Plan Breakdown:
1. ✅ [Complete] Install dependencies and start dev servers (`pnpm install && pnpm dev`)
2. ✅ Fix backend error handling for invalid platforms (404 middleware in server.js)
3. ✅ Add frontend URL validation guard in App.vue embedUrl computed
4. ✅ Implement liquid glass UI + animated background in style.css
5. ✅ Update App.vue and components with glass classes/transitions
6. ✅ Test fixes: Enter 'EmanuelPlays' modrinth user - error fixed (backend returns 404 "Invalid platform"), UI smooth glassmorphism + animated background applied
7. ✅ Restart dev servers (`pnpm dev`)
8. ✅ Complete: http://localhost:3000/docs (API docs), http://localhost:5173 (liquid glass UI with animated gradient bg, floating landing image, blur effects, smooth hovers/transitions)

**More ui stuff** 🚀
- More noticeable animated background (particles, shine sweep, 25% landing opacity)
- Staggered fade-in animations, glow pulses, shimmer buttons, neon borders
** i think it works** 🎉

Progress tracked here.
